package com.middleware.backend.notification.enums;

/**
 * Enum representing the type of notification channel.
 *
 * <p>Used to define the medium through which notifications are sent.</p>
 */
public enum ChannelType {

    /**
     * Email channel type.
     */
    EMAIL,

    /**
     * SMS channel type.
     */
    SMS
}
